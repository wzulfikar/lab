window: 
  enabled: true
  demomode: false # set true to disable quit from window (only quit from console)

# frame configuration
frame:
  # maxsize: 720,480 # optional: set frame max size (w,h)
  # minsize: 720,480 # optional: set frame min size (w,h)
  recordonstart: false
  flip:
    horizontal: false
    vertical: false

# server config (TODO)
# server: 
#   enabled: false
#   host: localhost
#   port: 8888

# where to store screenshots, video, etc
storage: ./storage

# db config (postgres) for python facerec
postgres:
  user: user
  pass: pass
  host: localhost
  port: 5434
  db: db